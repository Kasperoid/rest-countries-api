import { Flex, FlexProps } from 'antd';
import styled from 'styled-components';

export const HeaderContainerInnerStyled = styled(Flex)<FlexProps>`
  && {
    margin: 0 auto;
    height: 100%;
    max-width: 1440px;
    padding: 0 55px;
  }
`;
