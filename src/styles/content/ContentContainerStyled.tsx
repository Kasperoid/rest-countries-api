import { Layout } from 'antd';
import styled from 'styled-components';

const { Content } = Layout;
export const ContentContainerStyled = styled(Content)`
  && {
    width: 100%;
    padding: 35px 55px;
    max-width: 1440px;
    margin: 0 auto;
  }
`;
